#include "Retangulo.h"

Retangulo::Retangulo(double largura, double altura):FigBase(x, y, expessura, cor, tipo){ //Resolver isso...
    this->altura = altura;
    this->largura = largura;
    double f = x;
}

